




// const a1 = [1, 2, 3, 4, 5]

// const a2 = a1.filter((v) => v === 2)

// a2


// const a3 = a1.filter((v) => v === 2).map((v) => v * 2)

// a3


// const a4 = a1.flatMap((v) => v === 2 ? [v * 2] : []);

// a4


// const a5 = a1.map((v) => v === 2 ? v * 2 : []);

// a5


// // 假設我們有一個包含多個陣列的陣列
// const nestedArray = [[1, 2], [3, 4], [5, 6]];

// // 使用 flatMap() 將所有子陣列展平,並對每個元素進行平方運算
// const squaredElements = nestedArray.flatMap(arr => arr.map(num => num ));

// console.log(squaredElements); // 輸出: [1, 4, 9, 16, 25, 36]